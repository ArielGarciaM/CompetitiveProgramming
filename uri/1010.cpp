#include <bits/stdc++.h>
using namespace std;

int main()
{
    double a, b, c, d, e, f;
    cin >> a >> b >> c >> d >> e >> f;
    cout << "VALOR A PAGAR: R$ " << fixed << setprecision(2) << b*c + e*f << endl;
}